# Acceptance closure and native reliability plan

This historical plan is followed by the
[persistence/lifecycle milestone](PERSISTENCE-LIFECYCLE-PLAN.md). Its baseline and
results below remain tied to the original candidates.

Baseline inspected 2026-09-23: clean main/origin/main `2d4087e93289a21d797fab4cd641ecde6cf16a82`; PR #10 is OPEN at `b426bbdddd4d966fd6ce430996d2542bd7511aaf`, following qualified code `fd59c80195dee549ddbaaa23c28a06c091b18274`. Work only in `.worktrees/acceptance-closure`, branch `codex/acceptance-closure`. The new PR targets `codex/executable-qualification`; do not merge either PR. No `.codex/` instructions exist. Preserve the previous worktree and all raw evidence.

## Contract and ownership agreed before implementation

Keep schema 1 of the crosswalk and trusted producer packets unchanged. Preserve every original AC row and required mode, test-plan bytes, blocked release profile, and the separate Nuxt UI matrix. Add audited exact producer/file/name/mode links only; whole means the entire requirement in that mode, not a suite passing. Store classification and source/assertion audit in a new Markdown closure ledger, outside the strict crosswalk schema. New producer identities must be observed in real output before final claims. No speculative runtime correction.

| Owner | Files and bounded work | Closure criterion |
| --- | --- | --- |
| A, inventory/assertion audit | `docs/testing/acceptance-crosswalk.json`, `docs/development/ACCEPTANCE-CLOSURE-LEDGER.md` | Audit all 96 rows and required modes against normative companions and actual assertions. Classify existing unlinked evidence, missing assertion, missing implementation, native/manual/environment, external/owner prerequisite; exact source/tests and missing clauses. Coordinate B names before linking. Current producer output must match. |
| B, focused executable closure | Initially `tests/runtime/acceptance-closure.test.ts`, `tests/e2e/acceptance-closure.spec.ts`; propose any other exact test/runtime ownership to parent before edits | Select high-value implemented persistence/failure/concurrency/ownership gaps (AC-05/06/62/66/71 are candidates, not promised closures). Real services/actions, exact bytes/write counts, no success on failure, owned cleanup. Keep changes bounded and notify A of exact assertions. |
| C, native diagnosis | `docs/testing/NATIVE-RELIABILITY-INVESTIGATION.md`; new `scripts/testing/native-diagnostic-*.mjs` and `tests/tooling/native-diagnostic*.checks.mjs` only if justified | Start with first Windows Appearance and Linux pop-out failed reports. Record original identities, all attempts, observations and uncertainty. Define bounded read-only experiment before executing. No host launch until parent coordinates resources and explicitly provisioned isolated configuration. No rerun-to-green or timeout weakening. |
| Parent | This plan; shared schemas/config/workflows/package/ownership hashes; execution/review records, README/readiness reconciliation; candidate and evidence audit | Integrate, independently review actual diffs across owners, reproduce/fix findings, serialize heavy jobs, freeze and qualify corrected source/assets, push and deliver reviewable stacked PR. |

## Dependency and execution sequence

1. Complete baseline/read normative material; locate qualified Node 24.21.0/npm 11.19.1. Fresh exact-lock `npm ci --strict-allow-scripts` in new worktree. Inspect existing native raw evidence read-only while install runs.
2. A audits full inventory, B independently selects focused cases, C analyzes historical failures. Parent approves exact added ownership and shared changes. Workers must not run installs/builds/lint/coverage or native sessions concurrently; request the parent's execution slot.
3. Run focused real tests and negative controls; inspect actual producer registration names. Reconcile links with A. No count targets.
4. Separate cross-owner review wave reviews assertion validity, required modes, identities/failure retention, native causality/cleanup and generated-consumer/data preservation. Review actual diffs and reproduce concrete findings. Correct and rerun affected checks.
5. Run full `npm run verify`, served `npm run test:e2e` through the trusted browser producer, live `npm run check:security`, and selected `evidence run runtime|tooling|coverage|artifact|browser` producers as applicable. Provision browser explicitly. Preserve failed/interrupted runs and all samples. Exercise consumer/removal/archive when changes affect them.
6. Freeze corrected code commit; use existing fixed-source rehearsal/candidate infrastructure and retain source, candidate provenance and asset hashes. Changed source invalidates prior source evidence. Any asset reuse must compare hashes explicitly. Qualify frozen bytes; evidence-only documentation follows separately.
7. Recompute previous/current verified/partial/not-run counts from actual complete sessions. Update ledger, crosswalk, current execution/review records and PR description together. Commit imperatively, push, open/attach stacked PR, inspect/fix relevant checks. Main stays clean. Ask owner for next step after delivery.

## Diagnostic boundaries and delivery

Native experiments distinguish plugin, host, automation and unknown causes. Passing later runs do not explain prior failures. Never use personal vaults, force theme classes, stop unrelated processes, or assume an old idle window still applies. Prefer existing retained reports and source; any runtime fix requires a new candidate. Release/tag/upload/listing/permission changes and automerge are unauthorized.

Report exact advanced cases/reasons, all remaining grouped prerequisites, native eliminated hypotheses and unknowns, commands/results, code/evidence SHAs, retained asset hashes, PR/check state and an ordered next milestone. Historical 1 verified / 14 partial / 81 not-run is the comparison baseline, not implementation percentage or a target.

## Approved diagnostic integration amendment

Read-only audit found the original Linux errors have no driver timestamp or stable
page identity. Track C and parent agreed the optional `nativeDiagnostics` schema-1
supplement: driver wall/monotonic receipt times, stable page IDs, phase transitions,
page closes and indices into the unchanged errors array. Parent owns driver/helper
phase wiring, strict adapter field allowance and generated foundation template.
The helper owns only its listeners; every error still fails acceptance. The driver
also rechecks late errors after cleanup before its final report/exit. No product
runtime change, assertion deadline, host control or error filtering is introduced.
This instrumentation supports a future bounded comparison and does not establish
what caused the historical failures.

Independent review required strict validation of supplied diagnostic supplements
against the canonical errors array and unique attempt retention in the generated
foundation driver. Parent owns `native-diagnostic-validation.mjs` and its parser
regressions; C supplied the bounded actual-driver retention regression. Pre-fix
failures and corrected results are retained. Final heavy qualification runs in
the existing read-only fixed-source candidate and consumer/archive workflows;
local focused tests, full runtime/browser producers, strict types and gates run
before push. This avoids competing heavy jobs on the limited-RAM editing host.

## Integration state change during execution

The initial baseline above was observed correctly: PR #10 was open. It was merged
externally at 2026-09-23 19:52:23 UTC as
`3d9a46046e01509b5285c5d986d54e8a7be2c689`. A later live check confirmed that its
tree exactly equals `b426bbd`. Parent fetched/pruned, fast-forwarded clean main and
retargeted PR #11 to main. No merge was performed by this task and the frozen
`e14c1c3` candidate was not rebased/rebuilt. The specifically requested preservation
of the prior qualification worktree and raw evidence takes precedence over routine
worktree cleanup; both remain in their original paths.

## Follow-up correction after evidence-head checks

The evidence-only head's Windows showcase run failed an existing archive-analysis
fixture: its final `rm` reported `EBUSY` for the Git staging directory after about
61 seconds. Command outcomes were not retained, so whether an earlier error was
masked is unknown. Preserve run 35916883304 and
its downloaded artifact. A bounded owned-child control did not reproduce an orphan;
no Git/Fallow/process-leak cause is established. A distinct owned-lock control
examines primary-error masking before any correction.

Parent approved B's exact ownership of `tests/tooling/archive-command-fixture.mjs`,
`archive-command.checks.mjs` and only the archive case in `gates.checks.mjs`.
Retain command/result diagnostics and both primary/cleanup failures. Preserve all
analyzer assertions, the 60,000 ms/12 MiB command limits and current spawn behavior;
add no retries, blanket cleanup suppression or speculative process-tree changes.
Do not export environment/Git configuration secrets. Independent review and real
positive/negative controls precede a new frozen candidate. Earlier candidates and
failed checks remain distinct; their results cannot be relabeled as the new source.

## Final evidence delivery after external merge

PR #11 was merged externally at 2026-09-23 21:41:14 UTC as
`fc3400473d4183d3668d8525c6d5ed18a9b4667d`, before the completed evidence-only
commit was pushed. Its tree equals qualified code `e2758ae`. Parent confirmed the
merge, fetched/pruned and fast-forwarded clean main. Documentation-only PR #12
delivers the final records; the active worktree remains for that unmerged follow-up
and the explicitly preserved evidence. No PR merge was performed by this task.
