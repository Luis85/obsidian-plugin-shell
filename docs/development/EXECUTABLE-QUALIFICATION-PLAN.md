# Executable qualification milestone

Baseline: clean main `2d4087e93289a21d797fab4cd641ecde6cf16a82`, fetched 2026-09-23.
Worktree: `.worktrees/executable-qualification`; branch `codex/executable-qualification`.
No `.codex/` is present. PRs 6/8/9 merged; PR 7 closed without merging; PR 5 separate.

## Requirements, contracts and dependency order

| Track | Requirements / acceptance | Implementation and acceptance |
| --- | --- | --- |
| A | TST-08–11/15, AC-83–90; bounded AC-03/04/06/11/43/44/46 | Fixed trusted producers consume actual Vitest/Playwright/Node/native/artifact/coverage structures. Versioned packets bind full execution inputs before/after, exact tools/lock/config/protocol, cases/modes/counts/all attempts/environment/assets. Read-only check/report rejects incomplete, stale and unexpected evidence. Preserve all 96 original rows and modes through additive crosswalk, audited partial/whole links. |
| B | QLT-05/12 | Inspect installed Fallow 3.28.0 help and actual health/dupes schemas before configuring complete maintained-source inventories. Qualify proposed function limits 10 cyclomatic/15 cognitive and production duplication 3%, minimum 50 tokens/5 lines. Unsupported measurements remain gaps; narrow exceptions require owner/reason/review. Actual bad-code/report/inventory CLI controls. |
| C | NFR-02–05, AC-03/04/06/11 | Versioned native protocol: three warmups, 30 retained measured samples, monotonic clock, nearest-rank p95; warm plugin startup excludes host startup; 100-row finish requires real Items rendering. Twenty view cycles retain live sibling/resource baseline. Native Items CRUD/current state/bytes/write ownership and restart. Existing candidate size diagnostics never rebuild. |

1. Read live baseline, current implementation and normative requirements; fresh strict exact-lock install using Node 24.21.0/npm 11.19.1.
2. Agree contracts/ownership below before edits. Track A implements the parent-approved evidence schema; track C coordinates native report identity with A.
3. Implement independent tracks, then integrate commands, workflows and reviewed example ownership centrally. Serialize heavy checks and reserve uncontended performance execution.
4. Independent review wave crosses ownership boundaries; reproduce/fix concrete adversarial findings, then targeted verification and documentation/help polish.
5. Freeze code commit; hosted fixed-source rehearsal performs one accepted build plus full verify. Bind served/native/new producer evidence to retained bytes. Corrections invalidate candidate; preserve previous failures. Execute full renamed/edited consumer + maker/removal/post-removal workflow and literal archive scope.
6. Commit/push, open reviewable PR, inspect checks and correct failures. Evidence-only updates identify code and evidence revisions separately. No merge/publication/tag/permission change.

## Exact ownership

- A: `scripts/testing/evidence-*.mjs`, `tests/tooling/evidence*.checks.mjs` and dedicated fixture helpers; `docs/testing/acceptance-crosswalk.json`, `docs/testing/EXECUTABLE-EVIDENCE.md`. Original `test-plan.json` unchanged.
- B: `scripts/quality/check-maintainability.mjs`, `scripts/quality/maintainability-*.mjs`, `tests/tooling/maintainability.checks.mjs`, `docs/development/MAINTAINABILITY.md`. Production hotspot ownership assigned explicitly after measurement; no concurrent persistence/bootstrap edits.
- C: `scripts/testing/native-items.mjs`, `native-performance.mjs`, `performance-report.mjs`, integration in `check-native.mjs`; `tests/runtime/resource-qualification.test.ts`, `tests/tooling/performance-report.checks.mjs`, `docs/testing/PERFORMANCE-QUALIFICATION.md`. Additional helper files remain track-local and are reported to parent.
- Parent: package/workflow/configuration/dependency changes; shared contracts approval; example manifest/hashes/templates; README/PRD/readiness/current execution/review records; final integrated qualification and PR. No dependency change planned.

## Producer/report interfaces

`node scripts/testing/evidence-cli.mjs run <runtime|browser|tooling|coverage|artifact|native>` executes only checked-in producers and retains unique packets/raw reports. `check <packet>` verifies current identity; `report <packets...>` reconciles all 96 rows. JSON data never supplies commands or authorization. All failed/skipped/retried attempts remain nonpassing; a later success cannot erase earlier failures. Git clean/modified consumers and Git-free archives have content identities. Hashes are integrity bindings, not signatures or credentials for release operations.

Metric report records inventory/classification, exact tool/options/schema, actual functions/clones and denominators. Missing/unknown output or omitted source is an error. Generated consumer production source has the same gates. Existing dead-code/boundary and both coverage gates remain independent.

Performance report records protocol, environment, candidate assets, all samples including failures/invalid runs and separate reference/shared-runner status. Timings do not gate noisy CI; resource/correctness assertions block. Native host and synthetic adapter boundary assertions retain distinct modes.

## Verification and retained limits

Required: actual positive/negative producer/checker paths including faulty real runs; targeted tests; full `verify`; served E2E; live all-category audit; new commands; complete generated-consumer workflow; fresh checkout/archive; exact retained candidate hashes. Independent review targets false greens, schema drift, source/mode/asset mismatch, metric gaming, measurement finish conditions, data safety and cleanup. Record exact outcomes rather than expected success.

Full-template release readiness, event metadata expansion, device/manual breadth, supported nested ESLint graph, public operations and repository administration remain separate. Preserve unresolved historical Windows theme failure and every failed attempt. Ask owner for next step after delivery.
