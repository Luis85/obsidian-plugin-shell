# Executable qualification review

Three bounded implementation workers own evidence, maintainability and
performance/native qualification. Parent integration owns shared configuration,
schemas approval, candidate delivery and cross-cutting preservation. A separate
cross-owner review wave inspected the implementation and adversarial behavior;
implementation summaries are not independent-review evidence. The final
[execution record](../testing/EXECUTABLE-QUALIFICATION.md) binds the corrected
code to its actual candidate, consumer and native results.

The performance worker independently reviewed the maintainability implementation
and all 21 production refactor diffs. The evidence worker separately reviewed the
native/resource/performance implementation and parent build attribution. Review
found no behavioral regression in the inspected validation order, catches,
serialization, bus reentrancy, disposal or maker registration names. Execution
remains necessary and separately recorded.

| Finding | Correction and verification status |
| --- | --- |
| Staged metric corpora initially lacked the installed dependency context and Fallow reported infrastructure diagnostics | Preserve real installed context in isolated staging; reject diagnostics rather than treating a crash as zero findings. Actual minimal CLI reproduction identified the defect. |
| A real failing duplicate-code report could be changed to zero or one duplicated line while retaining clone groups; refreshed metadata then passed check mode | Added per-input eligibility receipts, exact clone line unions/redundant token totals, and gate-outcome reconciliation. Independent actual CLI recheck rejects the manipulated real output with `METRIC_REPORT_CLONE_TOTALS`. |
| Native restart checked only byte equality, so a redundant same-byte write would be invisible | Add a scoped call observer around restarted current-state queries and assert zero calls alongside unchanged bytes; restore it in finally. |
| Releasing a paused native save did not establish completion before scratch bytes were restored | Drain the owned save to quiescence before disabling/restoring. The shared driver helper's delayed-success and delayed-rejection regressions passed; primary and cleanup failures remain retained. |
| A real Vitest packet could contradict public reporter status while retaining passing JSON | Reconcile every suite/case status and count. The independent hash-refreshed tamper reproduction now fails with `EVIDENCE_STATUS`; nearby passing/fault/retry/repeat/skip controls pass their expected verdicts. |
| Fallow calls quoted/computed TypeScript methods named `<template>` template aggregates | Parse actual Vue template ranges with the selected Vue parser and validate template populations/locations. Actual CLI controls reject TypeScript and Vue-script impersonations while valid composition passes. |
| Performance classification, budget labels and size metadata could contradict otherwise valid raw samples | Validate allowed classification, derive the budget verdict again, and remeasure the existing candidate bytes/graph read-only. Focused contradictory metadata and host identity controls reject; these crafted parser fixtures do not claim native execution. |
| A Git failure could turn a broken checkout into an apparent archive | An owned `.git` marker now requires valid canonical Git root/revision/status. Actual malformed-metadata CLI control fails, and removing the marker restores the valid archive control. |
| Two genuine passing runs could register different cases under unchanged source | Session checking compares semantic case inventories/statuses/counts for each repeated producer. An actual ignored-marker registration change now fails the session while identical repetitions pass. |
| The document refactor reread projection getters after capturing their values | Validate the captured title/body primitives. A real regression first demonstrated false success for an invalid captured body; the corrected complete Documents/Items run passed 16/16 and independent review confirmed the original read order is restored. |
| The Items browser test title claimed deletion survived reload, but reload happened only before deletion | Add independent service reconstruction after every unit mutation and actual browser reload after create, rename and delete, with fault assertions before each reload. Targeted unit and served results passed; independent review approved only AC-03's two whole links. |
| The dependency-free source-limit fixture omitted the newly shared hash module | Add that required module to the isolated fixture's copy inventory. All 52 retained baseline cases then passed in each of three runs; thresholds, IDs and release blocking remain unchanged. |
| The generated UI create function exceeded the unchanged complexity gate | Extract its cohesive result presentation; the create function measures 8 cyclomatic/6 cognitive and the result helper 5/4. Real generated-source catalog, types and behavior checks passed, followed by the complete consumer workflow. |
| Literal archives exposed generated distribution files that Git checkouts implicitly ignored | Declare only the three generated distribution outputs in Fallow's ignore list. A transported Git-free archive passes; an extra source file in that directory still fails the real analyzer. Complete archive setup then passed in the consumer workflow. |
| A crafted native parser fixture inherited the removed consumer's foundation profile | Make the isolated fixture own its reviewed profile, prove the mismatch is rejected, then test the explicit showcase context. Both affected adapter cases passed without relaxing product validation. |

The reviewer also distinguished real native settings/item I/O from forced writer
interleavings. Controlled pending/failure scenarios must label their adapter
boundary explicitly; they cannot be presented as native disk faults. Historical
Windows theme failures remain unexplained despite later passing stock sessions.
