# Acceptance closure ledger

Current work: [framework lifecycle plan](FRAMEWORK-LIFECYCLE-PLAN.md) and
[execution record](../testing/FRAMEWORK-LIFECYCLE.md). Retained-action permits,
presence-aware reads, truthful uncertain guidance, UTF-8 bounds and independent
native observations strengthen selected clauses. Historical counts below do not
qualify the changed source; new links require actual producer output.

Thirty additive partial links now identify observed unit, component and served
recovery assertions. The second frozen session retains a failed native attempt and
therefore grants no acceptance advancement; source-bound reconciliation must pass
for the corrected candidate. Its separate foreign-notice fixture and launch-resource
protocol are qualification tooling, not new production capabilities or idle-host
performance proof.

Assertion audit of all 96 retained cases, 2026-09-23. This ledger supplements the
[crosswalk](../testing/acceptance-crosswalk.json) and preserves the
[normative machine inventory](../testing/test-plan.json). It is an implementation
and assertion inventory, not an execution report. Current observations require
the complete trusted producer session for the final source and policy bytes.
The historical comparison is **1 verified / 14 partial / 81 not-run** at
`fd59c80195dee549ddbaaa23c28a06c091b18274`; it is not implementation coverage.
At frozen code `e2758ae47723cc7496fabd0074b76b03db5c4ee2`, the complete Linux
producer session reports **2 verified / 54 partial / 40 not-run**, with all 96
crosswalk links observed. AC-03 and AC-06 are verified; a separate Windows CI
timeout remains unresolved. The [execution record](../testing/ACCEPTANCE-CLOSURE.md)
and [candidate audit](../testing/evidence/acceptance-closure-candidate.json)
retain exact source identities, all transitions and the qualification boundary.

The subsequent frozen code `7f14292871efb07ddace131fb9b5603de4122394` reports
**2 verified / 55 partial / 39 not-run**, with all 117 links observed. AC-71
advances from not-run to partial through PL-C71 and PL-B11-71; its native clauses
remain open. The other six selected rows gain stronger partial assertions without
whole-case promotion. See the [current execution record](../testing/PERSISTENCE-LIFECYCLE.md)
and [candidate audit](../testing/evidence/persistence-lifecycle-candidate.json).

Normative scope: [baseline AC-01–62](../product/BASELINE-0.4.md),
[AC-63–96 and companions](../product/SPECIFICATION-0.7.md), and current
[PRD amendments](../product/PRD.md). In particular, code-line counting supersedes
physical-line wording without changing limits; extracted host CSS has explicit
provenance and is not the original simulator; filenames retain the accepted title.
The [Nuxt UI acceptance matrix](../testing/NUXT-UI-ACCEPTANCE.md) remains separate.

Mode abbreviations below are exact: U = unit, C = component, B = browser-integrated,
N = native, T = tooling-generated, A = artifact, L = node-baseline,
H = http-specimen, S = browser-specimen, D = device, M = manual. Every required
mode is listed. A synthetic host, happy-dom DOM or release fake remains its
producer's declared mode. It cannot fill an N, M, D or generated-consumer gap.

Gap classes may coexist: **E** existing assertion needing current audited evidence;
**A** missing automated assertion or required-mode binding; **I** missing
implementation; **Q** native/manual/environment qualification; **X** external or
owner-controlled prerequisite. E never means the named test establishes the entire
row. The last column names the remaining work even where a bounded link is added.
Test IDs below identify assertions inside the exact named files, not suite passes.
Paths are repository-relative; `runtime/`, `e2e/`, `tooling/`, `verification/` and
`harness-styles/` in this table are under `tests/`.

| Case | Required modes | Gap classes | Inspected source and assertion boundary | Closure criterion / missing clauses |
| --- | --- | --- | --- | --- |
| AC-01 | T | E,A | `scripts/setup/identity.mjs`; `tooling/setup-identity.checks.mjs` IDENTITY-01 compares the entire dry-run snapshot and uses a nonexistent launcher; IDENTITY-02 preserves lock dependencies and unrelated metadata. | Bind one current differently named generated consumer through actual install/build/verify; CLI stubs and dry-run identity assertions alone do not establish readiness. |
| AC-02 | N | E,Q | `src/infrastructure/obsidian/commands.ts`; `runtime/commands.test.ts` COMMAND-03-03 coalesces equivalent dispatch; native command protocol in `scripts/testing/native-command.mjs`. | Actual candidate native command and ribbon opening/focusing, duplicate-registration and unload/reload assertions must all pass; unit dispatch is not native evidence. |
| AC-03 | U,B | E | `src/application/plugin-data-repository.ts`; existing exact Items links reconstruct services from saved envelopes and reload browser after each create/rename/delete. | Rerun both existing whole links together on current source; no native or release qualification is inferred. |
| AC-04 | C,B,N | E,A,Q | `src/features/items/definition.ts`; linked Items component/browser/native cases reject invalid English labels without writes. | Assert invalid inline guidance after switching to a supported non-default locale in every required mode, retaining zero-write checks. |
| AC-05 | U,B | E,A | PERSIST-05-01/02 assert exact failed note and uncertain shared-writer bytes, queued-write lockout, no success facts and reconstructed recovery. PL-B05 covers before/after browser persistence with retained prior record and drafts. Existing AC-CLOSE-05/66 retain create/open boundaries. | Current complete producer linkage plus complete absent/corrupt/inaccessible/future recovery contract remains necessary; known failure and uncertain acknowledgment must stay distinct. |
| AC-06 | U,B | E | `src/application/plugin-data-store.ts`; existing whole Items unit link overlaps preference/item transactions; `e2e/acceptance-closure.spec.ts` AC-CLOSE-06 pauses the item save while another view submits preferences, checks two exact envelopes, independent drafts, late-view and restored UI values. | Both whole links require passing current runtime/browser producer packets in one complete consistent session; controlled browser Storage remains the declared adapter boundary. |
| AC-07 | U,B | E,A | PERSIST-07-01/02/03 inspect root corruption/future versions, opaque registry/collection preservation and absent versus denied reads. PL-B07 adds served exact raw root preservation and protected collection preference writes. PL-C07 separately proves rendered disabled controls. | Broader ordered migrations and category-specific recovery remain unproved; do not infer them from protected storage or common error text. Every new link is partial and needs current producer output. |
| AC-08 | U,B | E,A | `harness/app` storage adapters; `runtime/browser-storage.test.ts` injected unreadable/uncertain storage assertions; `runtime/identity.test.ts` IDENTITY-01 checks renamed DOM/view identity. | Assert nonfatal local-preference denial and independent namespaces together in U/B; document-storage failure and DOM identity alone do not prove local preference fallback. |
| AC-09 | C,B,N | A,I,Q | `src/bootstrap` i18n composition; `e2e/showcase.spec.ts` UI-I05 persists German settings; `e2e/accessibility.spec.ts` A11Y-03 exercises narrow German controls. | Complete fallback/host-locale mapping, long text and native labels; pending locale-maker drafts are not reviewed supported languages. |
| AC-10 | C,B,N | E,A,Q | `src/application/modal-service.ts`; `runtime/components.test.ts` UI-03-11 checks cancelled/confirmed/prompt outcomes, invalid guidance and close-owner cleanup; `e2e/modals.spec.ts` MODAL-E2E-01 checks focus containment and return. | Audit full cancel/submit/reopen/focus/cleanup sequence in all modes, especially actual native dialogs; mocked native classes cannot satisfy N. |
| AC-11 | U,B | E,A,Q | Existing RESOURCE-20/HOST-20 count owner cycles/EventRefs. PL-B11-71 adds twenty served cycles with actual scheduler/dialog/notice/subscription/leaf counts and live sibling. | Window/pop-out ownership, partial startup and complete deferred resource behavior remain; zero ordinary action count is not proof of nonempty recovery registry cleanup. |
| AC-12 | T | E,A | `scripts/testing/code-lines.mjs`; `verification/source.test.mjs` SRC-01 counts full SFC/CRLF/literals, SRC-02 executes exact 100/400/450 boundary failures. | Existing baseline assertions have L scope, not T. Add a generated-source gate scenario covering full SFC and CRLF at/beyond the unchanged code limits. |
| AC-13 | T | E,A | `scripts/quality/check-architecture.mjs`; `runtime/tooling.test.ts` TOOL-I04 runs real Fallow and checks forbidden feature/infrastructure and bootstrap/test edges plus unclassified files. | Required T needs actual resolved alias/re-export/unclassified negative fixtures; the existing U tool invocation cannot be relabeled. |
| AC-14 | T,C | E,A | `tooling/gates.checks.mjs` GATE-02-02 requires real floating-promise, hardcoded Obsidian path and Vue v-for rule IDs with no parser failure; `scripts/quality/check-source.mjs` checks locale keys. | Add audited locale-negative assertions and component-mode defect checks; passing real lint negatives alone does not satisfy both modes. |
| AC-15 | T | E,A | `scripts/quality/check-analyzer.mjs`; `tooling/gates.checks.mjs` GATE-02-01 executes Fallow against unused file/export; ANALYZER-ARCHIVE rejects hidden maintained archived source. | Add a production export referenced only by tests and assert the actual gate still rejects it; ordinary unreachable exports do not establish the test-only reachability clause. |
| AC-16 | C,B,N | E,A,Q | `src/bootstrap/mount-ui.ts`; `runtime/components.test.ts` UI-03-08 records exactly vue.render independently with zero notes; evidence adapter controls reject genuine failed reporters. | Inject actual contained/uncontained browser/native defects and assert failed current artifacts; parser fixtures do not exercise the served UI or native renderer. |
| AC-17 | A,B | E,A | `scripts/quality/check-artifacts.mjs`, `scripts/bundling/vite-shared.mjs`; `e2e/showcase.spec.ts` UI-I01 checks no head style injection and unchanged host sentinel after disabling plugin CSS. | Bind exact CJS/complete composed assets, absence of extra chunks/dev code and served asset parity; sentinel containment alone is partial. |
| AC-18 | T,N | E,A,Q | `scripts/dev/install-local.mjs`; `tooling/build.checks.mjs` BLD-02-02 checks three installed assets and retained data.json; IDENTITY-INSTALL-01 preserves another installed plugin. | Add whole install snapshot assertions for notes, all unrelated plugins and security preferences; qualify actual contained native install separately. |
| AC-19 | T | E,A | `scripts/bundling/staged-build.mjs`, `scripts/dev/install-local.mjs`; BLD-02-01 in `tooling/build.checks.mjs` preserves complete last-good bytes on compile/incomplete output failures. | Combine unsafe destination/config and rollback/intervening-edit assertions with failed-build preservation in one whole audited T scenario; no destructive replacement. |
| AC-20 | L,N | E,A,Q | `scripts/testing/run-node-tests.mjs`; `verification/runner.test.mjs` RUN-02/03/04/05/06 reject failed/empty/skipped/malformed/hung execution. | Preserve baseline scope; native prerequisite-unavailable and actual host-crash reports must remain nonpassing with cleanup and candidate identity. |
| AC-21 | U,B | E,A | `src/infrastructure/diagnostics.ts`; `runtime/feedback.test.ts` LOG-I01 checks 200 retained records/210 observer calls and absence of literal private in the final retained record; declared catalogs are in `src/application/logging.ts`. | Audit all prohibited data categories before observation/export and no-upload behavior in both modes; one retained-substring assertion is insufficient. |
| AC-22 | C,B,N | E,A,Q | `e2e/accessibility.spec.ts` A11Y-01/03 checks real panels/dialog and German narrow forced-colors/reduced-motion keyboard use; theme protocol `scripts/testing/native-theme.mjs`. | Complete component and actual candidate native light/dark/narrow/keyboard matrix; browser simulation is not native Appearance evidence. |
| AC-23 | D | Q,X | `manifest.json` retains isDesktopOnly; no physical-device execution is established by desktop/browser evidence. | Keep mobile unclaimed. Any later iOS/Android claim requires separately provisioned candidate-bound physical-device qualification. |
| AC-24 | T | E,A | `scripts/examples/plan.mjs`; `tooling/example-removal.checks.mjs` reviewed deletion keeps consumer registration/files and idempotent rerun; modified registrations/bytes fail. | Qualify resulting consumer with no orphan catalogs/styles/locales/dependencies using complete verify/build and retained asset identity; deletion fixture alone is partial. |
| AC-25 | T | A,Q,X | `scripts/setup.mjs`, `scripts/makers/cli.mjs`, authoring guide; generated-consumer tests exercise commands, not a new developer's timed comprehension. | Record a measured fresh-developer first-change workflow without requiring the entire PRD; define observer/timing and actual successful output. |
| AC-26 | U,N | E,A,Q | `scripts/maintenance/discovery.mjs`; `tooling/maintenance.checks.mjs` records public and early-access separately, rejects prerelease stable metadata. | Current tests are T, not U/N; audit selected runtime API usage and actual minimum/public host with no silent beta-only dependency. |
| AC-27 | N | E,A,Q | `src/infrastructure/obsidian/settings-tab.ts`; `runtime/native-feature-settings.test.ts` checks focused canonical toggle, cached reopening, exact save counts and no setter on render under host fixture. | Actual native searchable/indexed settings, failed save and shared validated writer need candidate-bound assertions; fixture metadata is insufficient. |
| AC-28 | T | E,A,X | `tooling/npm-install.checks.mjs` NPM-03 pins approved hooks and rejects setup lifecycle recursion; qualified exact lock installation is separate execution evidence. | Representative supported update-family qualification remains; nested ESLint support exception cannot be closed by audit success or lock reproducibility. |
| AC-29 | U | E,A | `scripts/maintenance/discovery.mjs`; `tooling/maintenance.checks.mjs` checks current/update/cooldown/blocked states, all failed sources, owner/review timestamps. | Existing checks are T and cannot satisfy required U. Add an appropriate unit producer binding without relabeling tools or changing the normative mode. |
| AC-30 | N | Q,X | `scripts/testing/native-isolation.mjs` contains scratch host paths; normal setup has no required Obsidian CLI path. | If the optional CLI is claimed, explicitly provision it and prove every command targets only the configured fixture vault; isolation helper is not CLI execution. |
| AC-31 | T | E,A | `scripts/release/prepare.mjs`; `tooling/release.checks.mjs` prepare reviews five files read-only, applies version/history and rejects stale inputs. | Add explicit before/after Git refs/status and remote-mutation observation proving no implicit commit/tag/push/publication, alongside all consistent version files. |
| AC-32 | A,T | E,A,X | `scripts/release/candidate.mjs`, `promotion-plan.mjs`; `tooling/release-plan.checks.mjs` first/subsequent draft plans contain exact source and five assets with executed=false. | Retained package and fake-remote checks are partial; separately authorized real first/subsequent drafts must contain individually installable exact assets. |
| AC-33 | A,N,T | E,A,I,X | `scripts/release/execute.mjs`; `tooling/release-plan.checks.mjs` rejects missing acceptance/review, wrong host/platform/source/hash and changed bytes before operations. | Actual candidate native acceptance and trusted publication authorization/Actions surface remain separate; no rebuild and exact uploaded assets need authorized real qualification. |
| AC-34 | A,T | E,A,X | `scripts/release/execute.mjs`, `github-remote.mjs`; release execution/remote fixtures cover uncertain outcomes and protected published versions; plan tests reject duplicate assets/releases and wrong tags. | Execute authorized real permission/upload/resume failure matrix without touching public versions; synthetic remote success is not GitHub authorization evidence. |
| AC-35 | A,T | I,A,X | `scripts/setup/identity.mjs`, `scripts/release/prepare.mjs` reject malformed IDs/versions; listing checklist exists in release documentation. | Implement complete executable listing readiness for required docs/assets/current route; actual submission is owner-authorized external work. |
| AC-36 | T | E,A,Q | `scripts/setup/execute.mjs`; `tooling/npm-install.checks.mjs` NPM-04 uses stub npm/verify explicitly and NPM-05 proves dependency-free dry run. | Bind a fresh no-node_modules real setup through exact install, selected-profile verify and optional native provisioning; stub readiness cannot be whole. |
| AC-37 | T | E,A | `scripts/setup/options.mjs`; NPM-05 invokes dry run with nonexistent npm and asserts no node_modules; NPM-03 checks absence of all install/setup lifecycle hooks. | Add complete dependency-free help and noninteractive/TTY behavior to existing no-install/no-recursion assertions before whole linkage. |
| AC-38 | T | E,A | `scripts/setup/journal.mjs`, `execute.mjs`; `tooling/setup-identity.checks.mjs` IDENTITY-04/11 preserve reviewed bytes and concurrent edits rather than adopting them. | Link actual interruption, valid-only resume, failed recovery lock and explicit skipped-profile assertions together; safe identity planning alone is partial. |
| AC-39 | T | E,A | `scripts/quality/check-repository.mjs`; repository workflow tests reject unsafe permissions/triggers/unpinned actions; source inventory enforces code limits. | Assert thin root configs/workflows and scripts/helpers/templates layout using current generated output; unrelated workflow security checks are not a whole layout audit. |
| AC-40 | T | E,A | `scripts/makers/plan.mjs`; `tooling/maker-catalog.checks.mjs` MAKER-CATALOG applies every listed recipe, type-checks, runs generated runtime/tooling tests and verifies safe reruns. | This is finite generated-code evidence; bind each intended maker's complete registration/event/style behavior, then qualify the final generated consumer. |
| AC-41 | T | E,A | `scripts/shared/file-plan.mjs`; `tooling/maker-catalog.checks.mjs` MAKER-CONFLICTS preserves edited files and rejects stale messages/registration identity. | Include concurrent plan application, owned rollback failures and no-op reruns across maker mutations; edited-source rejection alone is partial. |
| AC-42 | T | E,A | `scripts/makers/engine.mjs`; MAKER-CATALOG generates and executes local custom reminder checks; MAKER-READ-SNAPSHOT rejects concurrent prerequisite changes. | Add explicit machine/noninteractive custom maker and no remote code/install assertions through the shared planner; local custom success covers only that branch. |
| AC-43 | U,B | E,A | `src/application/event-definition.ts`, registered descriptors; existing unit payload/catalog link plus `tooling/event-contracts.checks.mjs` actual compiler/checker negatives. | T compiler evidence cannot replace required U/B. Add served extension assertions and remaining typed key/union/duplicate rights coverage in appropriate modes. |
| AC-44 | U,B | E,A,I | `src/infrastructure/events/typed-event-bus.ts`; existing EVT-I01–06 assert order/removal, once-before-recursion, independent errors, frozen snapshots, disposal/isolation and depth bound. | Complete browser equivalents and retained EVT-10 bus-generated envelope metadata; payload-only snapshots do not implement event ID/time/instance/origin metadata. |
| AC-45 | N | E,A,Q | `src/infrastructure/obsidian/event-bridge.ts`; `runtime/host-events.test.ts` HOST-EVT-01/02/04 asserts exact file/folder/null/rename snapshots, eight refs and unload-before-ready under host fixture. | Execute these startup/null/rename/unload cases against real isolated native host; unit bridge tests cannot qualify N. |
| AC-46 | U,B | E,A | PERSIST-46-01 records exact committed envelopes/revisions and runtime isolation; PERSIST-05-02 rejects success facts on failed shared writes. PL-B46 covers actual two-view Items refresh and a separate runtime sharing storage without event crossover. | Maintain case-specific combined post-commit/failure/projection/isolation evidence. Each individual new assertion covers part of the row; no automatic promotion from multiple partial links. |
| AC-47 | A,B | E,A | `scripts/bundling/vite-shared.mjs`, `css-ownership.mjs`; UI-I01 checks served head/host containment; artifact checker inspects bundled CSS and assets. | Bind ordered source/SFC composition and exact single output to served bytes, self-contained imports/icons/fonts and no host leakage. |
| AC-48 | A,B | E,A,Q | `scripts/bundling/staged-build.mjs`; BLD-02-01 preserves last-good outputs, MAKER-CATALOG creates style source and safe reruns. | Add live style add/edit/remove HMR, failed rebuild and retained candidate parity in required A/B modes; source generator success is T only. |
| AC-49 | A,B | E,A,Q | `scripts/testing/code-lines.mjs`, `scripts/quality/check-artifacts.mjs`; source limits and output size gates are separate; UI-I01 probes host containment. | Explicit full SFC/CSS input versus composed output-budget assertions and actual pop-out style containment remain; browser scope cannot establish native breadth. |
| AC-50 | U,B | E,A | `src/domain/entity.ts`, `src/application/document-definition.ts`; `runtime/entities.test.ts` ENT-03-02/04/06 reject unknown/managed/null values, duplicate mappings/registrations and conflicting types. | Add required served-browser negative registration/input scenarios; compile-time type correlations in `runtime/types.ts` are not runtime producer assertions. |
| AC-51 | U,B | E,A | `src/domain/entity.ts`; ENT-03-01/02 asserts defaults, false/zero/absence/null rejection; `runtime/domain.test.ts` DOM-01 tests Gregorian leap years without timezone conversion. | Assert the complete default/date/falsy/null matrix via served real entity actions as well as U; UI task happy path does not cover all semantics. |
| AC-52 | U,B | E,A | `src/infrastructure/markdown.ts`; `runtime/entities.test.ts` YAML-03-01 round-trips ambiguous scalar values with actual YAML and exact CRLF body; ENT-03-04 rejects managed mapping overrides. | Add browser real YAML semantic/unsupported-value/managed-override negatives; exact displayed preview equality alone is not the whole scalar matrix. |
| AC-53 | U,B | E,A | `src/application/document-service.ts`; DOC-I01/02/03 asserts zero-write prepare, exact same path/Markdown, one concurrent write and stale/forged-plan rejection; UI-I02 compares persisted files to actual preview. | Browser needs explicit stale-plan and same identity/request assertions; existing read-only preview and exact persisted bytes support partial linkage. |
| AC-54 | U,N | E,A,Q | `src/infrastructure/obsidian/document-storage.ts`; DOC-I02 checks exact complete create args and no premature fact; DOC-I05 typed write failure has no success. | Bind real native complete create bytes and controlled failure with no empty-note workaround or premature fact, retaining one-write observation. |
| AC-55 | U,N | E,A,Q | `src/domain/paths.ts`, `src/infrastructure/obsidian/document-storage.ts`; DOM-04 rejects unsafe folders; document-filename/boundary tests retain title and reject aliases/conflicts. | Full native unsafe folder/file/case/race conflict and safe cleanup matrix requires current candidate evidence; no cross-process atomicity claim. |
| AC-56 | U,B | E,A | `src/application/document-service.ts`; DOC-I02 coalesces same plan and DOC-I03/04 rejects changed requests/uncertain retry; focused closure tests measure uncertain written bytes. | Browser still needs true overlapping duplicate submission and mismatched request-key assertions; disabled-button and uncertain outcome alone are partial. |
| AC-57 | U,B | E,A | `src/application/document-service.ts`; DOC-I07 and DOC-03-05 retain successful receipt after listener/publish throw with one write; UI-I04 preserves note and success after failed open. | Add served cache-lag/listener-error and host/service dedup observations; open-failure preservation covers one follow-up class. |
| AC-58 | U,B | E,A | `src/application/document-service.ts`; DOC-I06 creates a distinct meeting with false/zero scalars through the same service; `runtime/feature-extension.test.ts` exercises extension contracts. | Serve a second entity through generic infrastructure and assert Markdown authority, no automatic seed and no duplicate Task plugin-data collection. |
| AC-59 | T | E,A | `scripts/makers/plan.mjs`; MAKER-CATALOG generates domain/project/plugin-data entities, type-checks and executes generated tests. | Assert definition/action/fixture integration together with an unchanged user-vault snapshot; generated source is not evidence of user-note writes or their absence. |
| AC-60 | N | E,A,Q | `src/application/document-definition.ts`; ENT-03-06 rejects conflicting property semantics, and no automatic note migration is introduced. | Actual native existing-note bytes and vault-wide property-type configuration must remain unchanged across schema/type-hint startup and use. |
| AC-61 | U,B,N | E,A,Q | `src/infrastructure/markdown.ts`; DOC-I01 parses actual YAML, UI-I02 compares actual emitted Markdown to preview; `scripts/testing/native-repository.mjs` handles native note inspection. | Candidate-bound native Properties and Source semantics on every claimed platform remain; generated strings/host doubles are not native Properties evidence. |
| AC-62 | U,B | E,A | PERSIST-62-01 proves discard versus in-flight runtime disposal for committed/failed/uncertain notes; PERSIST-62-02 proves 100 retained requests/discard slot reuse/selected export privacy. PL-C62/PL-B62 close a writing view, retain exact Markdown and sibling draft, observe committed event and explicitly reload notes. | Complete privacy/input/template/collision bounds and window ownership remain; string length is not a UTF-8 byte budget. Separate Task editor retained actions need a disposal audit. |
| AC-63 | U,B | E,A | `src/domain/outcome.ts`, `src/infrastructure/diagnostics.ts`; DOC-I04 asserts uncertain effect, DOC-03-04 unexpected/none and safe code/operation; logging tests constrain declared metadata. | Full category/effect/correlation/report identity matrix including non-Error/cross-realm throws needs U/B assertions; strings in notices do not prove canonical failure identity. |
| AC-64 | C,B,N | E,A,Q | `src/presentation/composables/use-document-form.ts`; UI-03-01 and UI-I02 focus invalid title and retain zero notes; Items component assertions retain inline guidance. | Assert associated persistent fields/summary and zero per-field toast burst in all modes; visible alert alone does not prove association or notification count. |
| AC-65 | C,B,N | E,A,Q | `src/application/notification-policy.ts`; `runtime/notification-policy.test.ts` NTF-03-01/02 asserts one updateable sink, progress delay and distinct attempts under U. | Add real component/browser/native per-operation handle observations including progress-to-terminal replacement; U policy checks do not satisfy C/B/N. |
| AC-66 | U,B | E,A | Existing AC-CLOSE-66 proves uncertain-create lockout and open-only single flight. PERSIST-66-01 guards retained disposed view navigation/preview/commit/reset/open with real call counts and frozen state. PL-C66/PL-B66 cover stale/disposed pending open completions. | Keep active/pending/disposed outcomes distinct and qualify all required clauses together. Safe host opening is follow-up only; retained Task repository-editor actions remain a separate follow-up. |
| AC-67 | U,B | E,A | `src/application/notification-policy.ts`; NTF-03-08 records sink/update/hide/observer/translation errors independently and preserves inline recovery; REL-02-04 bounds diagnostic subscriber failures. | Add browser reporter/translator/sink failure combinations with exact independent code/scope/count and no recursive notifications; selected unit sink fallback is partial. |
| AC-68 | C,B,N | E,A,Q | `src/bootstrap/mount-ui.ts`; UI-03-08 renders owned fallback and independently records exactly vue.render with no notes. | Deliberate contained Vue defect must fail an ordinary served/native scenario through independent ledger, with current failed artifacts retained; component containment alone is partial. |
| AC-69 | U,B | E,A | `scripts/testing/fault-ledger.mjs`; `verification/ledger.test.mjs` exact faults/overflow negatives and evidence-cli overflowing child control are baseline mode. | Add real U/B ledger negative assertions for missing/extra/count/scope/overflow; baseline parser/control evidence cannot be relabeled. |
| AC-70 | C,B,N | E,A,Q | `src/application/notification-policy.ts`; NTF-03-03 asserts three visible/ten queued/FIFO/one overflow, NTF-SAFE-03/04 retains essential recovery and observes drops. | Required component/browser/native burst/dedup/expiry/dismiss tests must retain recovery and independent evidence; U fake-timer policy is supporting evidence only. |
| AC-71 | C,B,N | E,A,Q | PL-C71 invokes actual sink callbacks and invalidates pending runtime recovery on disposal. PL-B11-71 measures served owned timers/dialogs/notices over twenty cycles, retaining sibling and unrelated DOM. | Native owned notices/actions/timers and foreign Notice preservation remain missing; current native diagnostics are a bounded production buffer, not the independent fault observer. Browser ordinary actions are zero and DOM sentinel is not a foreign native Notice. |
| AC-72 | C,B,N,M | E,A,Q,X | `src/presentation/components`; UI-03-10 exposes persistent keyboard recovery, MODAL-E2E-01 checks focus containment/return, A11Y-03 keyboard/narrow/media. | Manual actual screen-reader/live announcement and native recovery/timing review remain mandatory; axe/ARIA assertions cannot substitute. |
| AC-73 | N | E,A,Q | `src/infrastructure/obsidian/notification-sink.ts`; NTF-03-09 models persistent legacy handles and inline actions with one owner timer; public Notice limitations are documented. | Qualify real public Notice create/update/hide and accessible owning-view recovery; never assume private manager or target-window APIs. |
| AC-74 | A,B | E,A | `scripts/styles/vendor-policy.mjs`, `harness/styles/vendor/provenance.json`; TOK-06/09 records unknown version, upstream rights, excluded fonts and no remote CSS; UI-I01 tests served containment. | Bind artifact exclusion and explicit extracted-versus-original profile labeling; do not call extracted upstream CSS original authored simulation. |
| AC-75 | A,B | E,A | `scripts/quality/check-artifacts.mjs`, `scripts/styles/token-contract.mjs`; TOK-04 rejects missing/deprecated aliases; legacy browser specimen has missing-style checks. | Add wrong identity and missing host/plugin CSS negatives for actual served production components and package shim rejection; specimen/Baseline tests do not fill B/A. |
| AC-76 | C,B,N | E,A,Q | `e2e/accessibility.spec.ts` A11Y-03 labels German narrow forced-colors/reduced-motion; `e2e/layout-and-header.spec.ts` UI-02-SCALE explicitly labels simulated scaling. | Complete long-text/theme/media cases in every required mode and retain exact real-host identities; simulated scaling cannot prove native zoom. |
| AC-77 | L | E,A | `scripts/testing/test-plan.mjs`; `verification/policy.test.mjs` POL-06 leaves specimen-to-native row partial and release blocked; RPT-04 excludes coverage/releaseReady from XML. | Retained baseline must remain visibly separate; add exact trusted L control link only after auditing full specimen-versus-service/component labels. |
| AC-78 | N | E,A,Q | `scripts/testing/native-identity.mjs`; NATIVE-ID-01 derives selectors from candidate and NATIVE-ID-03 reads manifest without writes under T. | Actual native comparison needs resolved host/installer/candidate identity or explicit unverified state; manifest-derived selectors are not proof of executing host version. |
| AC-79 | T | E,A | `scripts/makers` templates and MAKER-CATALOG generated tests use shared services; generated tests execute after recipe compilation. | Audit generated canonical failure/notification mapping and real cleanup assertions for each relevant maker; generator success count alone is not whole. |
| AC-80 | A,N | E,A,X | `scripts/release/promotion-plan.mjs`, `scripts/testing/evidence-acceptance.mjs`; release-plan negatives reject missing native acceptance and wrong hashes. | Retain blocked release; exact artifact/native freshness and authorized promotion must reject specimen-only input. Fake review packets do not grant authority. |
| AC-81 | H | E,A | `scripts/harness/serve-style-fixture.mjs`; `harness-styles/server.test.mjs` HTTP-01–11 runs loopback, exact bodies, traversal/verbs/CSP/no-store and archive exclusion. | Existing retained H baseline links remain separate; a trusted H producer is not provided by runtime/tooling/browser adapters. Preserve platform execution scope. |
| AC-82 | T | E,A | `scripts/makers/plan.mjs`; MAKER-CATALOG runs a finite explicit recipe list and generated tests, NPM-03 rules out install/setup recursion. | Assert generated qualification does not recursively generate more projects or omit required finite scenarios; fixed test count alone does not establish policy completeness. |
| AC-83 | L | E,A | `scripts/testing/test-plan.mjs`; `verification/policy.test.mjs` POL-01–05/07 rejects missing/duplicate IDs, unsafe paths, empty/retry/fabricated tests and partial multi-link success. | Preserve legacy links; add a current trusted L assertion covering the entire inventory rejection contract rather than promoting one fixture's happy path. |
| AC-84 | L | E,A | `scripts/testing/report.mjs`; `verification/report.test.mjs` RPT-01/02 excludes duration from semantic digest and rejects failed first run/drift; evidence-cli repeated-run control retains inventory drift. | Existing linked producer repetition control remains partial: it does not establish complete repeated cold-run determinism across the retained baseline suite. |
| AC-85 | L | E,A | `scripts/testing/fault-ledger.mjs`; `verification/ledger.test.mjs` ERR-01–06 compares exact expected faults and overflow; evidence-cli deliberately overflowing assertion cannot pass. | Full exact code/scope/count/missing/extra and overflow contract must be observed in trusted L evidence; one overflowing child is only partial. |
| AC-86 | L | E,A | `scripts/testing/evidence-identity.mjs`; existing evidence-cli fresh-input link rejects changed source, added tests, missing packets and invalid modes. | Retain partial scope unless the complete modified/new-input hashing and stale-report contract is asserted together in current L producer output. |
| AC-87 | L | E,A | `scripts/testing/evidence-adapters.mjs`; existing real CLI links reject failed/skipped/empty/source-changed and malformed packet cases. | Complete crash/missing reporter-result scenarios and all empty/skip/malformed variants need whole audited binding; no automatic promotion from suite status. |
| AC-88 | L | E,A | `scripts/testing/evidence-runner.mjs`; existing evidence-cli hung producer test verifies timeout verdict and retained failure. | Preserve partial unless all owned descendant cleanup observations establish bounded termination; timeout classification alone is not proof of no surviving process. |
| AC-89 | L | E,A | `scripts/testing/evidence-acceptance.mjs`, `report.mjs`; existing CLI case checks execution counts and acceptance with blocked release; RPT-04 XML excludes coverage/releaseReady. | Assert source coverage, acceptance and execution denominators remain distinct in actual current report, including partial/not-run rows. |
| AC-90 | L | E,A | `scripts/testing/evidence-acceptance.mjs`; existing CLI blocked guard and POL-06 native mode substitution control. | Complete baseline/inline/specimen rejection in release/native requirements; keep release permanently blocked in this milestone. |
| AC-91 | L | E,A | `scripts/styles/check-tokens.mjs`; `verification/tokens.test.mjs` TOK-01 verifies >900 observed/>120 reviewed names and 38 aliases; TOK-03 rejects global defaults. | Preserve narrow retained token-baseline scope; current trusted L linkage must inspect sourced reference and scoped aliases together. |
| AC-92 | L | E,A | `scripts/styles/vendor-policy.mjs`; TOK-02 flips a byte to require SNAPSHOT_MISMATCH and ensures only named vendor archive has no line limit; TOK-11 rejects concatenated archive. | Bind exact integrity/exception behavior in current L output; do not exempt other vendor-looking files or infer host compatibility. |
| AC-93 | L | E,A | `scripts/harness/style-profile.mjs`; TOK-05 compares host/plugin/gallery link positions; TOK-07/08 checks explicit extracted/simulated profile isolation. | Whole requires load order plus isolated profiles with no fallback; retained single load-order link is not the entire assertion set. |
| AC-94 | S | E,A,Q | `scripts/testing/check-browser-specimen.mjs`; retained BRW-07/10/11 test stylesheet specimen overrides/missing styles, explicitly separate from served app. | Rerun in actual declared S environment and retain its own artifacts; B producer cannot substitute merely because both use Chromium. |
| AC-95 | L | E,A | `harness/styles/vendor/provenance.json`; TOK-06 asserts unknown app/native comparison, byte identity, fonts=false, production=false and no MIT relicensing. | Current provenance assertions must remain bound to exact vendor input; do not turn unknown source version into a native comparison claim. |
| AC-96 | L | E,A | `scripts/styles/token-contract.mjs`; TOK-04 rejects misspelling/deprecated/missing tokens, TOK-10 rejects duplicate aliases. | Bind current complete alias drift negatives under L; preserve exact token grammar and avoid claiming general CSS lint coverage. |

## Persistence/lifecycle assertion extension

The [current plan](PERSISTENCE-LIFECYCLE-PLAN.md) and [separate review](PERSISTENCE-LIFECYCLE-REVIEW.md) extend the seven selected rows above. The previous frozen counts remain historical; the complete current session is audited above. New unit, component and served assertions have deliberately partial extents; every remaining required mode and normative clause stays visible.

## Ordered finish list

1. Preserve this completed assertion audit and qualify every later source change
   afresh. Current runtime/browser producers observed the focused persistence,
   concurrency and recovery tests; AC-06's stronger whole link replaced its prior
   partial browser link. AC-05/66 remain partial. Resolve the retained Windows
   coverage timeout through bounded diagnostics, without extending its deadline.
2. Close implemented behavior with missing assertions: corrupt/future browser
   storage, typed entity/YAML browser negatives, lifecycle resources, locale/modal
   breadth, diagnostic redaction and contained-defect controls. Keep controlled
   adapters, generated consumers and real hosts distinct.
3. Resolve producer-mode gaps explicitly (AC-12/13/26/29 and retained baseline
   families) without editing normative modes or relabeling existing evidence.
4. Implement separately scoped missing capabilities: EVT-10 envelope metadata,
   full listing validator and trusted Actions publication surface; do not expand
   this persistence milestone into those projects.
5. Finish native reliability diagnosis before broader native qualification, then
   measured onboarding, manual accessibility and any owner-requested device scope.
   Dependency support and repository/release administration remain external.

Every existing/new link is conditional on current producer output. No collection
of partial links is promoted by coverage, a test title, historical prose or a
later native success. Native failed attempts remain governed by the separate
investigation record, including unresolved Windows Appearance and Linux pop-out
causality. Release remains blocked and no publication is authorized.
