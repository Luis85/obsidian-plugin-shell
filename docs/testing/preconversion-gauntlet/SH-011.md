# SH-001 / SH-011 execution and review record

## Scope and candidate binding

2026-09-24. Source recovery and live refs are in the [inventory](../../development/SHELL-CAPABILITY-INVENTORY.md).
This record applies to the source tree in its containing commit and the explicit
source-hash manifest beside it. Later CI is attributed separately to exact heads;
pending CI is not passed evidence. Local review used separate sequential passes,
not separate workers or an independent human reviewer.

## Evaluation contract established before implementation

SH-001: reconcile current main/concept/planning, actual implementation versus missing
shared capabilities, all task dependencies, all 96 AC/34 NUI IDs and retained native,
security and quality restrictions. Do not promote historical evidence.

SH-011: data-only discovery must enumerate actual built-in maker/operation handlers,
detect metadata drift, reject unsupported schema/versions and invented execution,
remain independent of consumer configuration, and preserve the existing maker CLI.
Versioned request/progress/result/error/receipt validation must have strict bounds.
The fixture must poison real consumer scripts and exercise a Git-free independent
copy, not merely inspect strings. Actual generation still requires existing maker
and full consumer suites under the pinned toolchain.

## Executed local evidence

Environment: Linux x64, Node v22.16.0, npm 10.9.2. This differs from the repository's
qualified Node 24.21.0/npm 11.19.1 pair. These dependency-free results are supplemental.
No local dependency install, full runtime/coverage/build or native result is claimed.

| Command / attempt | Actual result | Scope |
| --- | --- | --- |
| Initial direct git access | DNS resolution failed | No remote checkout or authentication claim; authenticated GitHub connector used instead. |
| Extract source archives, git add/write-tree | Both reconstructed trees match remote objects exactly | Source identity only; no execution claim. |
| node --test tests/tooling/capability-discovery.checks.mjs, initial red | Exit 1: missing catalog module, test file cannot load | Retained red control before implementation, not 10 executed failing behaviors. |
| Same command, first implementation | Exit 0, 10/10 | Actual Node CLI subprocesses, real temporary directories, independent copy and protocol cases. |
| Same command, review expansion | Exit 1, 12/13 | Test-helper defect discovered; original failure retained. |
| Same command, corrected explicit-input review | Exit 0, 13/13 | Negative cases and source/handler metadata checks; no native execution. |
| node scripts/quality/check-source.mjs | Exit 0, 495 inputs, 191 locale keys after adding task tests | Source code-line and locale parity only; input count changes with subsequent files. |
| Initial node scripts/testing/verify-baseline.mjs --repeat 3 --json | Tool time budget terminated before any verdict | Empty log is not a failed test, pass, or completed baseline; later complete run recorded separately. |

The combined discovery/task-graph command passed 16/16 checks; the graph includes all
44 task IDs and preserves all 96 AC/34 NUI routes. After byte-for-byte reconstruction
of uploaded tree `d515be9612729fe0d04732fb729bde2f0f80a48e`, the 13 discovery checks
were rerun successfully and committed as `afbc4119f34639db8d1b6e62c7632a7ed1f1a3ce`.

[Initial red](capabilities-red.txt), [first pass](capabilities-first.txt),
[review failure](capabilities-review-red.txt), [corrected review pass](capabilities-review-green.txt).
Absolute workspace prefixes are replaced with <repository>; failures are not deleted.

The later 180-second baseline attempt also terminated without a report or verdict.
No baseline acceptance is advanced. Both runs left empty logs.

The CAP-12 test helper had a default input = {}. Supplying undefined to that helper
therefore tested a valid empty input instead of the intended invalid value. A separate
explicit-input probe showed undefined -> error, omitted input -> error and {} -> result
before the correction. The correction constructs {...request(), input:value}; it changes
no expected behavior and weakens no production guard. Independent review of this test
correction remains outstanding. UTF-8 decoding was also tightened to reject malformed
bytes rather than replacing them; fixed INTERNAL_ERROR reporting distinguishes damaged
installed metadata from malformed requests.

## Per-criterion assessment

SH-001 source/task coverage is complete as an inventory, not product qualification.
The routing file contains no acceptance status; original IDs, modes, reports, lockfile
and thresholds remain intact. The graph regression checks current task frontmatter
and gate ancestry with deliberately cyclic/missing/duplicate fixtures.

SH-011 catalog/schema parity and no-consumer-execution behavior pass the supplemental
local probes. All message families have executable validators and documented version,
unsupported-operation and receipt boundaries. Unsupported shared features are visibly
planned, with no execution transports. Existing help/list and argument parsing pass.
Full existing maker generation/independent consumer, pinned-toolchain CI and independent
review remain required: **SH-011 stays in-review**, not fully qualified.

## CI qualification and residual restrictions

The only existing workflow change adds build/preconversion-shell to the existing
exact-candidate qualification branch filter. Its tool pins, steps, native repetitions,
thresholds, read-only permissions, preserved failures and publication boundary are
unchanged. This enables source-bound shell qualification, not companion conversion.
The existing release:rehearse step runs verification and retains local candidate bytes;
it does not mutate a remote release, create a tag or authorize publication.

Do not infer native behavior from the catalog tests, copied source or compiled output.
The general dependency support exception and historical host failures remain open.
No screen-reader, participant usability, macOS/mobile, controlled-reference performance
or complete cross-platform claim is made here. SH-022 and CX-007 are blocked.
