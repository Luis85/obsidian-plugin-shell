---
task_id: SH-026
phase: shell
priority: P0
status: planned
depends_on: [SH-013, SH-025]
plan_refs: [E01, E05, E11]
---
# SH-026 — Assemble the runnable framework developer kit

## Outcome
A GitHub-release-ready archive includes a working TypeScript-derived CLI before any dependency or plugin installation.

## Baseline and scope
Create a finite reviewed distribution manifest and compiled shell.mjs with matching schema/template/shared modules. Include bootstrap package aliases, exact baseline, notices, compatibility and integrity metadata. This packages an asset, not a public release.

## Acceptance criteria
- [ ] Extracted node shell.mjs setup/help and npm run setup work without node_modules, Git metadata, companion, global CLI or a CLI build step.
- [ ] The asset is explicitly assembled, not an automatic repository source archive; packed dependencies resolve without unpublished workspace or machine paths.
- [ ] Artifacts exclude credentials, approvals, host profiles, consumer-specific private designs, test vaults, maintainer state and unintended companion assets.
- [ ] Every artifact/module version and hash is recorded; deterministic inputs produce reproducible artifacts under documented build rules.
- [ ] Archive path/link/size rules and licenses are verified; network prerequisites and lack of fully offline dependency installation are clearly disclosed.

## Verification
Inspect/extract the real asset in clean Windows/macOS/Linux fixtures; test bootstrap routes and tamper/missing-module rejection. Full downstream journey is SH-032.

## Evidence and handoff
Not executed. Record exact commits/artifacts, commands/results, environment and remaining scope. Documentation is not implementation evidence or execution authorization.
