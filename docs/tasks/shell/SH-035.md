---
task_id: SH-035
phase: shell
priority: P0
status: in-review
depends_on: [SH-015, SH-018]
plan_refs: [E04, E06, E07]
---
# SH-035 — Compile companion JSON into a shell implementation workspace

Status: implementation under verification. Related issue: #19. Depends on PR #5's full project JSON, shared file-plan engine and retained shell foundation.

## Outcome

A developer exports the companion self-project, plans/applies a complete independent implementation workspace, and begins business logic with prepared source contracts, Pinia stores, UI components and TDD files. This task belongs before native companion concept conversion and publication.

## Acceptance

- Preserve the legacy byte-exact, no-write handoff.
- Provide shared TypeScript compilation through direct CLI and npm entrypoints.
- Generate source types/guards, services/ports/adapters, isolated Pinia stores and host/UI wiring from the export.
- Retain every PRD requirement and its acceptance text in a linked use-case/TODO test; never fabricate acceptance success.
- Execute generated contract, navigation and lifecycle tests; independently install and build the generated companion.
- Plan before applying, bind writes to a reviewed hash, preserve consumer changes and reject stale/unowned/unsafe targets.
- Keep full framework/native/release gates independent of scaffold verification.

## Explicit follow-on work

Convert rich components/editors and free-form interaction specifications into actual native features; implement persistence/payload mappings; make each TODO a meaningful passing behavioral test; adapt product-specific host expectations and satisfy full quality/native/release gates. The generated shell is not the completed companion plugin.

Implementation and commands: `docs/development/COMPANION-GENERATOR.md`. Verification workflow: `.github/workflows/project-generator.yml`.

## Identity and delivery-plan reconciliation — 2026-09-25

This is PR #20's original compiler task, formerly also named SH-023, associated with [issue #19](https://github.com/Luis85/obsidian-plugin-shell/issues/19). It was integrated into the current base at `1dfa991df77ea5fa742d8bd5e300c877d0d45151`. It is assigned SH-035 solely to resolve the concurrent ID collision; SH-023 in PR #18 retains the different baseline/CI task. `in-review` preserves the original “implementation under verification” status; this merge does not close its acceptance criteria.

The recorded dependencies route outstanding interchange and ownership qualification. [SH-028](SH-028.md) depends on this implemented compiler baseline and extends it into the complete framework-first journey rather than rebuilding it. Existing behavior still uses a separate vault-relative target, runtime TypeScript execution and fixed framework `src`/`tests` roots; compiled archive bootstrap and complete configurable-root support remain broader roadmap work.

## Verification and evidence handoff

Use the [generator guide](../../development/COMPANION-GENERATOR.md), [generator workflow](../../../.github/workflows/project-generator.yml), and exact-source results. Preserve every acceptance statement above, requirement TODO and native/release limit. The conflict repair preserves all incoming executable files byte-for-byte; its focused local tests are recorded in [PR18 merge reconciliation](../../testing/PR18-MERGE-RECONCILIATION.md). They are not a replacement for qualified-toolchain, independent generated build or native evidence.
