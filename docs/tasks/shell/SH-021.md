---
task_id: SH-021
phase: shell
priority: P0
status: planned
depends_on: [SH-001, SH-013]
plan_refs: [E11]
---
# SH-021 — Close supported-dependency and security gaps

## Outcome
The shell and exported consumers have an explicitly supported, qualified dependency/runtime matrix and reproducible safety checks.

## Baseline and scope
Inspect the exact lockfile, CI/toolchain configuration, [dependency exception](../../development/ITERATION-TWO-DEPENDENCY-EXCEPTION.md), readiness ledger and security checks. Recheck current primary maintainer documentation and advisories when executing; do not assume the planning snapshot is current.

## Acceptance criteria
- [ ] Declared Node/npm, type packages, build APIs, Obsidian API/host minimum and supported platforms are reconciled with actual tests.
- [ ] The known nested-linter support issue is resolved through compatible supported changes or remains an explicit blocking requirement; a clean audit is not support evidence.
- [ ] Runtime and development dependency changes retain exact locks, peer correctness, licenses and existing quality floors.
- [ ] Consumer archives exclude secrets/private state and pass relevant path, HTML, process and dependency safety tests.

## Verification
Run strict install, applicable linters/types/tests/analyzers, independent export qualification and a live all-category audit. Registry/network errors stay failures or blocked evidence, not zero findings. Test the declared minimum environment rather than only one convenient toolchain.

## Evidence and handoff
Not executed. Record sources checked, exact versions, command results and unresolved support/platform blockers. No automatic global install or publication.
