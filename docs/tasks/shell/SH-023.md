---
task_id: SH-023
phase: shell
priority: P0
status: in-progress
depends_on: [SH-001]
plan_refs: [E01, E09, E11]
---
# SH-023 — Reconcile the implementation baseline and Windows CI

## Outcome
One inspected source baseline contains the existing capability work and passes required integration checks before the new CLI migration begins.

## Baseline and scope
Read the [PR #5 review](../../development/PR5-FRAMEWORK-READINESS-REVIEW.md). Reconcile PR #17 discovery/protocol/maker dispatch, gauntlet inventory, task evidence and tests with PR #5 project-transfer and newer runtime changes. Preserve the concurrent b0f50ecb Windows test-portability repair and verify the corrected setup-matrix/showcase checks; investigate any remaining failure. This is not a blanket old-branch merge or a new discovery implementation.

## Acceptance criteria
- [ ] Inventory current branch ancestry and content; preserve useful PR #17 implementation rather than recreate it. Record what remains intentionally unintegrated.
- [ ] Restore/reconcile capability and task-graph tests, metadata, analyzer entries and existing status/evidence without copying stale README ownership or package baselines.
- [ ] Retain original Windows logs and the b0f50ecb repair evidence; verify its canonical-path/CRLF regression coverage and correct any remaining defect without relaxing dependencies, guards, retries or test scope to conceal failure.
- [ ] All applicable exact-head integration workflows complete successfully; skipped downstream steps and later green runs do not erase original failed evidence.

## Verification
Replay actual discovery/maker negative controls and both qualified/alternate Windows setup environments; rerun Linux counterparts, template consumers and concept transfer. Link exact failed and corrected heads.

## Evidence and handoff
Not executed. Record exact commits/artifacts, commands/results, environment and remaining scope. Documentation is not implementation evidence or execution authorization.

Convert rich components/editors and free-form interaction specifications into actual native features; implement domain-specific persistence policies and any undeclared payload mappings; make each TODO a meaningful passing behavioral test; adapt product-specific host expectations and satisfy full quality/native/release gates. The generated shell is not the completed companion plugin.

## Concurrent task-ID reconciliation — 2026-09-25

PR #18 and PR #20 independently introduced different tasks as SH-023. This ID retains the baseline/CI reconciliation task and its existing dependents. The compiler task from PR #20 / issue #19 is preserved as [SH-035](SH-035.md), including its acceptance scope and implementation-under-verification status. Historical SH-023 references in PR #20 or issue #19 refer to SH-035; no implementation or evidence is discarded.

## Implementation checkpoint — 2026-09-25

Selective PR #17 discovery/maker integration is present. The latest v3 companion base is merged. Complete pinned-platform CI and historical gauntlet evidence reconciliation remain separate. See [CLI workflow](../../development/FRAMEWORK-CLI.md) and [execution record](../../testing/FRAMEWORK-CLI-IMPLEMENTATION.md). This checkpoint does not close the task or waive any original acceptance criterion.

Implemented semantic Vue layout generation, typed primitive component contracts, safe
variant overrides, named slots, state/ancestor visibility, bound source projections,
navigation/business-hook dispatch and generated detail/acceptance tests. The current
self-project is the generated-project qualification fixture. See the generator guide.
Unimplemented business semantics remain explicit hooks/TODOs, not passing acceptance.

## Declarative boundaries increment — 2026-09-25

Implemented detail-schema-2 authoring/export, typed control semantics, bounded JSON/Markdown text editing, slot-content ownership and duplication, declarative source/emission payloads, and explicit native note CRUD through the canonical repository. Reconciles current PR #5 including its scoped Design System compiler. Added actual-repository/Markdown and combined-registry tests plus independent synthetic boundary project qualification.

Remaining product work is not replaced by dispatch tests: port the actual visual editors, supply custom provider/auth/domain rules, turn each PRD TODO into a behavioral acceptance test, and execute native Obsidian qualification. Details: `docs/development/GENERATOR-DECLARATIVE-ACTIONS.md`.

## Provider, relationship and recipe increment — 2026-09-25

Implemented on PR #21: enabled recipe compilation into the retained seeded test-data engine and reviewed .test-vault kit; canonical metadata/fixture-read tests; complete source-provider overrides with unload/setup-failure cleanup; typed origin-approved HTTPS JSON providers; and shared native relationship preflight with restrict deletion, cardinalities, target resolution and cancellation. The synthetic boundary fixture now exercises a writable self relationship plus an API recipe. Database drivers and business PRD acceptance remain developer-owned; no native or publication claim follows from generator tests.

Implementation contract: [providers and relationships](../../development/GENERATOR-PROVIDERS-AND-RELATIONSHIPS.md). The existing dedicated generator workflow independently installs, builds and tests both the actual companion export and the synthetic boundary fixture.

## Reconciled local review — 2026-09-25

Integrates the previously local shared manifest/compiler, canonical fixture metadata, typed full fixture ports, defensive queued note snapshots, read-only relationship audits and browser-export-to-generator parity. Preserves concurrent HTTPS providers, complete-source overrides, responsive composition, scoped styles, and strict whole-graph relationship semantics. Uses one `testdata:*` command family and one relationship session implementation. Adds independent provider-fixture qualification. See `docs/development/GENERATOR-FIXTURES-AND-RELATIONSHIPS.md` for exact policy decisions and verification limits.

## Baseline patch integration — 2026-09-25

Ported the remaining generator-boundary baseline patch behavior onto PR head `7d87754`. It adds cross-adapter idempotent canonical creation, accessor/symbol-safe native note input, a repository-consistent request-ID contract, own-property schema lookup and duplicate relationship-definition refusal. Strict whole-graph relationship enforcement remains the default, and mandatory cycles are tested as unsupported. The integration also corrected three defects that pre-date it: a shared fixture translator that exhausted its input budget on live browser state, a generated relationship test missing connected repositories, and a Windows `.test-vault` root check that rejected 8.3/drive-letter aliases. The remaining Windows file-symlink cases now skip explicitly when the OS denies them. Record: `docs/development/GENERATOR-BASELINE-PATCH-INTEGRATION.md`. Generator/scaffold qualification is not business or native Obsidian acceptance.
