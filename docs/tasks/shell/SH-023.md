# SH-023 — Compile companion JSON into a shell implementation workspace

Status: implementation under verification. Related issue: #19. Depends on PR #5's full project JSON, shared file-plan engine and retained shell foundation.

## Outcome

A developer exports the companion self-project, plans/applies a complete independent implementation workspace, and begins business logic with prepared source contracts, Pinia stores, UI components and TDD files. This task belongs before native companion concept conversion and publication.

## Acceptance

- Preserve the legacy byte-exact, no-write handoff.
- Provide shared TypeScript compilation through direct CLI and npm entrypoints.
- Generate source types/guards, services/ports/adapters, isolated Pinia stores and host/UI wiring from the export.
- Retain every PRD requirement and its acceptance text in a linked use-case/TODO test; never fabricate acceptance success.
- Execute generated contract, navigation and lifecycle tests; independently install and build the generated companion.
- Plan before applying, bind writes to a reviewed hash, preserve consumer changes and reject stale/unowned/unsafe targets.
- Keep full framework/native/release gates independent of scaffold verification.

## Explicit follow-on work

Convert rich components/editors and free-form interaction specifications into actual native features; implement persistence/payload mappings; make each TODO a meaningful passing behavioral test; adapt product-specific host expectations and satisfy full quality/native/release gates. The generated shell is not the completed companion plugin.

Implementation and commands: `docs/development/COMPANION-GENERATOR.md`. Verification workflow: `.github/workflows/project-generator.yml`.

## Detailed-design compiler increment — 2026-09-25

Implemented semantic Vue layout generation, typed primitive component contracts, safe
variant overrides, named slots, state/ancestor visibility, bound source projections,
navigation/business-hook dispatch and generated detail/acceptance tests. The current
self-project is the generated-project qualification fixture. See the generator guide.
Unimplemented business semantics remain explicit hooks/TODOs, not passing acceptance.
