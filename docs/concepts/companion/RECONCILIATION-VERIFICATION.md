# Concept 10: executed reconciliation verification

Execution date: 2026-09-23. This record qualifies the browser concept, not the production template or an installed Obsidian companion.

## Accepted artifact

- File: `docs/concepts/companion/index.html`
- Size: **1,062,056 bytes**
- SHA-256: **`d25dd5a892c09760a202f1663e2d14f8a6d2567ae91ca5ce3b920e3dc99207d7`**
- UI label: `Concept 10 · Reconciled workspace`
- Merge checkpoint: `76ab0cb5cfd7b0c1c66d1042f52b41e9895e9482`, with main `2d4087e93289a21d797fab4cd641ecde6cf16a82` as its second parent.

## Actual local results

| Command / scope | Result |
| --- | --- |
| `python3 scripts/concepts/build-companion.py --check` | Exact byte-for-byte reproduction passed. |
| `node --check` on every authored `docs/concepts/companion/src/*.js` | 42 files passed; local Node 22.16.0, not the template's qualified production toolchain. |
| `python3 tests/concepts/companion-reference.browser.py` | 50/50 scoped checks passed. |
| `python3 tests/concepts/companion-reference-graph.browser.py` | 8/8 scoped checks passed. |
| `python3 tests/concepts/companion-reconciliation.browser.py` | 34/34 scoped checks passed. |
| Observed runtime output across these suites | Zero page/console errors; zero runtime network requests. |

All three browser records identify the accepted hash above. Total: **92 current scoped checks**. Model, controlled-concurrency and synthetic beforeunload checks are labeled separately in the generated JSON records; the total is not 92 physical pointer journeys.

The reference suite records actual editing, preview synchronization, save/undo/redo, library placement, sections, validation, navigation and narrow layouts. The graph suite exercises real handle dragging, SVG-line editing, card dragging and on-card sorting. The reconciliation suite includes nested-dialog shortcut protection, field focus, exact removal restoration, export, limit behavior, popover dismissal and 1600/1024/390-pixel layouts.

## Corrections and historical evidence

An initial run of the older library suite stopped at its obsolete assertion that a card contains exactly three compact actions. The reference-led design intentionally uses a floating selection toolbar. That older suite was not certified, suppressed or counted as passing. The former 480-check total remains historical and is not repeated for this candidate.

The reference drag test was corrected to aim at the lower half of the destination block when testing insertion after it. This preserves actual drag coverage while matching the new explicit before/after semantics. An initial draft-removal banner disappeared during library/preview redraw; the final candidate restores that banner from its retained draft state. Final tests were rerun after the corrections.

## Reproduction and limits

Tests used pre-provisioned Python Playwright and Chromium, injecting the exact HTML with `set_content`. Direct file navigation returned `ERR_BLOCKED_BY_ADMINISTRATOR`. Controlled model mutations represent external changes; they are not native compare-and-swap proof. Synthetic beforeunload cancellation is not a claim that every browser displays the same confirmation.

No actual project scripts, native Obsidian instance, personal vault, package install, production generator, download service, release or marketplace submission was exercised. File-origin storage, physical touch/pen/trackpad, screen-reader coverage and complete accessibility compliance remain unverified. These results do not upgrade the template's existing native or release evidence.

The branch workflow separately checks deterministic assembly and JavaScript syntax. Its status is recorded by GitHub, not inferred from this local record. The temporary artifact-assembly helper is retired after the accepted generated bytes are committed; final verification workflows remain read-only.
