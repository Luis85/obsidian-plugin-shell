---
description: Project rules for {{name}}
alwaysApply: true
---

Follow AGENTS.md in the repository root: it defines the commands that must pass (`npm run check`), the architecture rules and the safety rules.
