diff --git a/docs/concepts/companion/README.md b/docs/concepts/companion/README.md
index 971b074..973031c 100644
--- a/docs/concepts/companion/README.md
+++ b/docs/concepts/companion/README.md
@@ -2,3 +2,3 @@
 
-> **One vault, one project · product audit and workflow/recovery polish — 2026-09-24.** Interactive browser concept, not an installable native Obsidian companion.
+> **One vault, one project · full-project JSON, companion self-project and read-only shell handoff — 2026-09-24.** Interactive browser concept, not an installable native Obsidian companion.
 
@@ -6,3 +6,9 @@ Open [index.html](index.html) in a desktop browser. Scripts, styles, icons and t
 
-## Current product audit
+## Project JSON and companion self-project
+
+Choose **Load companion project** to review and load the companion's own authored design into the one-project workspace. Export the full design to JSON, import a file or pasted JSON with explicit replacement, and configure **Preferences → Configure project folders** (`src` / `tests` by default). **Prepare → Project JSON → shell** shows the read-only v1 CLI: it returns the provided export without generating files.
+
+[PROJECT-JSON.md](PROJECT-JSON.md) explains the UI, seed content and limits; [companion-project.json](companion-project.json) is the importable self-project. The [shell contract](../../development/COMPANION-PROJECT-JSON.md) documents the command and safety boundary. This extends the existing single-project model, not a multi-project launcher.
+
+## Previous product audit
 
diff --git a/docs/concepts/companion/src/base.html b/docs/concepts/companion/src/base.html
index 96838df..fcb92a8 100644
--- a/docs/concepts/companion/src/base.html
+++ b/docs/concepts/companion/src/base.html
@@ -214,5 +214,5 @@ function switchDialog(){return vaultContextDialog();}
 function attachDialog(){return vaultContextDialog();}
-function settingsDialog(){return dialogBody('Workbench preferences',`<div class="grid2"><div class="card"><h3>Appearance</h3><p>Host-inspired dark and light themes. No fonts or assets are fetched.</p><div class="button-row">${button(state.settings.theme==='dark'?'Switch to light':'Switch to dark','theme','','','sun')}</div></div><div class="card"><h3>Local demo state</h3><p>Only this concept’s inputs are stored in your browser. No actual workspace is connected.</p><label class="checkbox"><input type="checkbox" data-field="remember" ${state.settings.remember?'checked':''}><span>Remember the demo on this browser</span></label></div></div><div class="mt24">${callout('This concept is English-only. Production requirements include English/German, validated settings, machine-local trust and platform-specific executable paths.')}</div>${storageWarning?`<p class="error">${esc(storageWarning)}</p>`:''}<div class="button-row">${button('Export this session','storage-recovery','','','download')}${storageWarning?button('Export retained browser data','storage-retained','','','download'):''}${button('Review scenario controls','lab','','','settings')}${button('Reset demo state','reset','','danger','refresh',state.activeRun?'disabled':'')}</div>`);}
+function settingsDialog(){return dialogBody('Workbench preferences',`<div class="grid2"><div class="card"><h3>Appearance</h3><p>Host-inspired dark and light themes. No fonts or assets are fetched.</p><div class="button-row">${button(state.settings.theme==='dark'?'Switch to light':'Switch to dark','theme','','','sun')}</div></div><div class="card"><h3>Local demo state</h3><p>Only this concept’s inputs are stored in your browser. No actual workspace is connected.</p><label class="checkbox"><input type="checkbox" data-field="remember" ${state.settings.remember?'checked':''}><span>Remember the demo on this browser</span></label></div></div>${companionSettingsCard()}<div class="mt24">${callout('This concept is English-only. Production requirements include English/German, validated settings, machine-local trust and platform-specific executable paths.')}</div>${storageWarning?`<p class="error">${esc(storageWarning)}</p>`:''}<div class="button-row">${button('Export this session','storage-recovery','','','download')}${storageWarning?button('Export retained browser data','storage-retained','','','download'):''}${button('Review scenario controls','lab','','','settings')}${button('Reset demo state','reset','','danger','refresh',state.activeRun?'disabled':'')}</div>`);}
 function paletteDialog(){return dialogBody('Find an action',`<label class="small muted" for="palette-search">Search workspace actions</label><input autofocus class="palette-input" id="palette-search" placeholder="Type an action or screen…" data-field="palette-search" autocomplete="off"><div id="palette-results" class="modal-list">${paletteResults('')}</div><p class="scope-label mt16">Ctrl / ⌘ K to open · arrow keys to move · Enter to choose · Escape to close</p>`);}
-function paletteResults(q){const rows=[['vault-init','','Project details','Identity for this vault’s one project','settings'],['vault-prepare','','Prepare this vault','Review additive template files and build tools','terminal'],...NAV.map(([v,ic,t])=>['palette-nav',v,t,'Open workspace view',ic]),['storage-recovery','','Export this session','Private recovery snapshot; saved design only, not form drafts','download'],['tour','','Resume guided tour','Explain the workbench','help'],['lab','','Review scenarios','Exercise failure and recovery states','settings']].filter(a=>(a[2]+' '+a[3]).toLowerCase().includes(q.toLowerCase()));return rows.length?rows.map(([a,v,t,d,ic])=>`<button data-action="${a}" data-value="${v}">${icon(ic)}<span>${t}<small>${d}</small></span></button>`).join(''):'<p class="no-results">No matching action.</p>';}
+function paletteResults(q){const rows=[['project-example','','Load companion project','Review an editable definition of the companion itself','box'],['project-import','','Import project JSON','Review a full portable project before replacing this one','file'],['project-export','','Export project JSON','Complete saved authoring data and folder settings','download'],['project-handoff','','Project JSON to shell','Version one prints the supplied JSON only','terminal'],['vault-init','','Project details','Identity for this vault’s one project','settings'],['vault-prepare','','Prepare this vault','Review additive template files and build tools','terminal'],...NAV.map(([v,ic,t])=>['palette-nav',v,t,'Open workspace view',ic]),['storage-recovery','','Export this session','Private recovery snapshot; saved design only, not form drafts','download'],['tour','','Resume guided tour','Explain the workbench','help'],['lab','','Review scenarios','Exercise failure and recovery states','settings']].filter(a=>(a[2]+' '+a[3]).toLowerCase().includes(q.toLowerCase()));return rows.length?rows.map(([a,v,t,d,ic])=>`<button data-action="${a}" data-value="${v}">${icon(ic)}<span>${t}<small>${d}</small></span></button>`).join(''):'<p class="no-results">No matching action.</p>';}
 function activationDialog(){const p=project();return dialogBody('Generated plugin activation',`<div class="dialog-simulation">Simulated host settings — not real Obsidian</div><h3 class="mt16">Authoring vault: ${esc(CONCEPT_VAULT.name)}</h3><p>Shell Workbench is already installed and enabled here. This is not evidence that the plugin you are developing has been installed or tested.</p><h3 class="mt24">Isolated test vault: ${esc(vaultDisplayTarget())}</h3><div class="context-path">${esc(p?.vault||vaultTestRoot())}/.obsidian/plugins/${esc(p?.id||'my-plugin')}/</div><div class="card mt16"><h3>${esc(p?.name||'Your plugin')}</h3>${vaultProjectPrepared()?button(p.enabled?'Disable in demo':'Enable in demo','enable-demo',p.id,p.enabled?'small':'primary small',p.enabled?'stop':'play'):badge('Prepare this project first')}</div><p class="scope-label mt16">Changes browser fixture state only. No Restricted Mode change, real enablement or native qualification occurs.</p>`,button(state.wizard?.step===7?'Return to preparation':'Back to workspace',state.wizard?.step===7?'return-wizard':'close','','ghost'));}
diff --git a/docs/concepts/companion/src/interaction-polish.js b/docs/concepts/companion/src/interaction-polish.js
index ab86dfb..da1a508 100644
--- a/docs/concepts/companion/src/interaction-polish.js
+++ b/docs/concepts/companion/src/interaction-polish.js
@@ -2,4 +2,5 @@
 let modalOriginal=null;
-const TRACKED_FORMS=new Set(['style-guide-form','test-data-form','data-source-form','semantic-form','component-variant','vault-identity','design-transfer','edge-detach','ref-content','ref-section','library-place','library-upgrade','brick-edit','brick-transfer','connection-create','connection-structure','flow-intent','flow-binding','design-node','design-connect','design-goal','product-prd','product-requirement','product-component','product-bind']);
+const TRACKED_FORMS=new Set(['project-import','project-folders','style-guide-form','test-data-form','data-source-form','semantic-form','component-variant','vault-identity','design-transfer','edge-detach','ref-content','ref-section','library-place','library-upgrade','brick-edit','brick-transfer','connection-create','connection-structure','flow-intent','flow-binding','design-node','design-connect','design-goal','product-prd','product-requirement','product-component','product-bind']);
 function formCheckpoint(){
+ if(modalType==='project-import')return JSON.stringify(projectTransferUi.text);
  if(modalType==='style-guide-form')return JSON.stringify(sgUi.form?.record||null);
diff --git a/docs/concepts/companion/src/vault-project.js b/docs/concepts/companion/src/vault-project.js
index 1443552..5e05da5 100644
--- a/docs/concepts/companion/src/vault-project.js
+++ b/docs/concepts/companion/src/vault-project.js
@@ -15,3 +15,3 @@ function validVaultScope(s,validateProject){
  return s.schema===2&&s.vaultKey===CONCEPT_VAULT.key&&!['projects','activeId','designDraft'].some(k=>Object.hasOwn(s,k))&&validVaultFiles(s.vaultFiles)&&typeof s.legacyHandled==='boolean'&&
-  (s.project===null||(validateProject(s.project)&&s.project.key==='vault-project'&&s.project.root===vaultRoot()&&[vaultTestRoot(),vaultRoot()+'/.dev-vault'].includes(s.project.vault)&&['planning','prepared'].includes(s.project.phase)&&typeof s.project.projectNote==='string'&&(!s.project.design||validSavedDesign(s.project.design))));
+  (s.project===null||(validateProject(s.project)&&validCompanionProjectFolders(s.project.folders)&&s.project.key==='vault-project'&&s.project.root===vaultRoot()&&[vaultTestRoot(),vaultRoot()+'/.dev-vault'].includes(s.project.vault)&&['planning','prepared'].includes(s.project.phase)&&typeof s.project.projectNote==='string'&&(!s.project.design||validSavedDesign(s.project.design))));
 }
diff --git a/docs/concepts/companion/src/vault-workspace.js b/docs/concepts/companion/src/vault-workspace.js
index 9b6bc21..a0020c3 100644
--- a/docs/concepts/companion/src/vault-workspace.js
+++ b/docs/concepts/companion/src/vault-workspace.js
@@ -5,3 +5,3 @@ function vaultLocationCard(){return `<section class="vault-location"><span class
 function vaultWelcomeView(){return heading('This vault. One project.','Start with the idea. Prepare the development toolchain when the design is ready.')+vaultLocationCard()+
- `<section class="start-workspace"><div class="start-head"><div class="eyebrow">YOUR PROJECT WORKSPACE</div><h2>Design first.<br>Build when you are ready.</h2><p>Your empty folder is already open as an Obsidian vault, and the companion is installed. Define this project, capture requirements and shape its screens before obtaining the template.</p><div class="row wrap">${button('Start designing','vault-init','','primary','layers')}${button('Use example outline','sample','','ghost','box')}</div><p class="start-disclosure">No Node, npm, Git or account is needed for design. This browser concept does not detect a real vault or write native files.</p></div><div class="start-process" aria-label="Vault-first project journey">${[['check','Open a folder as a vault','Completed before entering the plugin.'],['file','Define this project','A brief, requirements and a connected design.'],['layers','Shape the experience','Sitemap, layouts, content and shared components.'],['terminal','Prepare this vault','Review additive template files; then install and verify.']].map(([ic,t,d],i)=>`<div class="start-process-row"><span class="process-icon">${icon(ic)}</span><span><strong>${t}</strong><small>${d}</small></span><span class="process-order">${i+1}</span></div>`).join('')}</div></section>${vaultLegacyBanner()}<footer class="start-footer"><span>For a different project, open a different vault using Obsidian.</span>${button('Take a tour','tour','','ghost small','help')}</footer>`;}
+ `<section class="start-workspace"><div class="start-head"><div class="eyebrow">YOUR PROJECT WORKSPACE</div><h2>Design first.<br>Build when you are ready.</h2><p>Your empty folder is already open as an Obsidian vault, and the companion is installed. Define this project, capture requirements and shape its screens before obtaining the template.</p><div class="row wrap">${button('Start designing','vault-init','','primary','layers')}${button('Use example outline','sample','','ghost','box')}${button('Load companion project','project-example','','ghost','box')}${button('Import project JSON','project-import','','ghost','file')}</div><p class="start-disclosure">No Node, npm, Git or account is needed for design. This browser concept does not detect a real vault or write native files.</p></div><div class="start-process" aria-label="Vault-first project journey">${[['check','Open a folder as a vault','Completed before entering the plugin.'],['file','Define this project','A brief, requirements and a connected design.'],['layers','Shape the experience','Sitemap, layouts, content and shared components.'],['terminal','Prepare this vault','Review additive template files; then install and verify.']].map(([ic,t,d],i)=>`<div class="start-process-row"><span class="process-icon">${icon(ic)}</span><span><strong>${t}</strong><small>${d}</small></span><span class="process-order">${i+1}</span></div>`).join('')}</div></section>${vaultLegacyBanner()}<footer class="start-footer"><span>For a different project, open a different vault using Obsidian.</span>${button('Take a tour','tour','','ghost small','help')}</footer>`;}
 function vaultLegacyBanner(){return legacyAvailable()?`<section class="callout warn"><div class="grow"><strong>Previous multi-project concept data found</strong><p>The original remains untouched. Bring one outline into this vault, or export the old workspace before deciding.</p></div>${button('Review old workspace','vault-legacy','','small')}</section>`:'';}
@@ -12,3 +12,3 @@ function vaultPreparationView(){
  `<section class="next-task"><span class="next-task-icon">${icon('terminal')}</span><div class="grow"><h2>${vaultProjectPrepared()?'This project is prepared':'Your design is already a project'}</h2><p>${vaultProjectPrepared()?'Continue in the existing workspace; initialization will not create another project.':'Preparation adds the template here. It does not create a second folder or replace the current design.'}</p></div>${button(vaultProjectPrepared()?'Open development':inProgress?'Resume preparation':'Review preparation',vaultProjectPrepared()?'nav':'vault-prepare',vaultProjectPrepared()?'develop':'','primary','arrow')}</section>
- <div class="grid2"><section class="card"><h2>Preserved in this vault</h2><p>Obsidian configuration, the companion installation, Project.md and your requirements, sitemap and components.</p><div class="context-path">${esc(CONCEPT_VAULT.config)}/ · Project.md · project/</div><p class="small muted">Existing files are compared before writes. Conflicts stop preparation; there is no force-overwrite option.</p></section><section class="card"><h2>Added after review</h2><p>Template source, build scripts and locked dependencies. Runtime testing uses an isolated vault, not the project’s authoring vault.</p><div class="context-path">src/ · scripts/ · package.json<br>Test target: ${esc(vaultDisplayTarget(vaultTestRoot()))}/</div><p class="small muted">Template acquisition, source writes, dependency installation, testing and activation are separate stages.</p></section></div><div class="button-row">${button('Back to the design','nav','sitemap','','layers')}${button('Project details','vault-init','','ghost','settings')}${button('Project record','vault-record','','ghost','file')}</div>`;
+ <div class="grid2"><section class="card"><h2>Preserved in this vault</h2><p>Obsidian configuration, the companion installation, Project.md and your requirements, sitemap and components.</p><div class="context-path">${esc(CONCEPT_VAULT.config)}/ · Project.md · project/</div><p class="small muted">Existing files are compared before writes. Conflicts stop preparation; there is no force-overwrite option.</p></section><section class="card"><h2>Added after review</h2><p>Template source, build scripts and locked dependencies. Runtime testing uses an isolated vault, not the project’s authoring vault.</p><div class="context-path">src/ · scripts/ · package.json<br>Test target: ${esc(vaultDisplayTarget(vaultTestRoot()))}/</div><p class="small muted">Template acquisition, source writes, dependency installation, testing and activation are separate stages.</p></section></div><div class="button-row">${button('Project JSON → shell','project-handoff','','primary','terminal')}${button('Back to the design','nav','sitemap','','layers')}${button('Project details','vault-init','','ghost','settings')}${button('Project record','vault-record','','ghost','file')}</div>`;
 }
diff --git a/docs/concepts/companion/src/workflow-views.js b/docs/concepts/companion/src/workflow-views.js
index 9dc83fa..7d62e84 100644
--- a/docs/concepts/companion/src/workflow-views.js
+++ b/docs/concepts/companion/src/workflow-views.js
@@ -10,3 +10,3 @@ function workflowOverview(){
  const f=workflowFacts(),n=workflowNext();
- return heading(f.p?.name||'Your plugin workspace','The current vault is your project root. Design and source stay together.',button('Project details','vault-init','','small','folder'))+
+ return heading(f.p?.name||'Your plugin workspace','The current vault is your project root. Design and source stay together.','<div class="row wrap">'+button('Project details','vault-init','','small','folder')+button('Export project JSON','project-export','','small','download')+button('Import project JSON','project-import','','small','file')+'</div>')+
  `${vaultLocationCard()}<section class="next-task"><span class="next-task-icon">${icon('arrow')}</span><div class="grow"><h2>${n.title}</h2><p>${n.description}</p></div>${workflowActionButton(n)}</section>
